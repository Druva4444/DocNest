{
  "name": "docnest",
  "version": "1.0.0",
  "description": "**DocNest** is a fully-featured, production-ready cloud storage platform tailored for document upload, access, and subscription-based usage. Built with a modern full-stack architecture, it supports secure Google OAuth login, AWS S3-backed file storage, and flexible Stripe billing for monthly plans.",
  "main": "index.js",
  "scripts": {
    "build":"npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start":"npm run start --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
